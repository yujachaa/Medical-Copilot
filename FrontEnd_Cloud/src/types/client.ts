export type client = {
  id: number;
  comName: string;
  grade: string;
  key: string;
  availability: boolean;
  totalCount: number;
  createDate: string;
  subscription: number;
};

export type clientDetail = {
  id: number;
  comName: string;
  grade: string;
  key: string;
  availability: boolean;
  createDate: string;
};
